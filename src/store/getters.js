export default {
  // GET_FILM: (state) => {
  //   return filmId => {
  //     return state.films.find(film => film.id === filmId)
  //   }
  // }
  GET_FILM: (state) => state.films,
  GET_TV_SHOWS: (state) => state.films
}
